
let request = require('supertest');
let config = require('config');
const User = require('../../../models/user');
const Profile = require('../../../models/profile');
const mongoose = require('mongoose');

describe(`Company route - /company`, ()=>{
    describe(`GET`, ()=>{

    });
});